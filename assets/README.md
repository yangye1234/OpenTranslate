# App Icon Assets

Put your app icon PNG here as:

- `assets/app-icon-1024.png` (recommended size: 1024x1024)

Then run:

```bash
./scripts/generate_app_icons.sh
```

This generates:

- `assets/icons/app.icns` for macOS bundle icon
- `assets/icons/app.ico` for Windows executable/installer icon
